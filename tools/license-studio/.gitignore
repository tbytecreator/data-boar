# Never commit keys, blobs, or local audit DBs
*.pem
*.key
*.db
*.sqlite
/bin/
/dist/
# Built binary at tool root only (do not use pattern `studio` — it would ignore cmd/studio/*.go)
/studio
studio.exe
.audit-secret
