---
- name: Skip toolchain restriction (opt-in)
  when: not (t14_toolchain_restrict_enabled | bool)
  ansible.builtin.debug:
    msg: "Toolchain restriction disabled (t14_toolchain_restrict_enabled=false)."

- name: Apply toolchain restriction
  when: t14_toolchain_restrict_enabled | bool
  block:
    - name: Ensure toolchain group exists
      ansible.builtin.group:
        name: "{{ t14_toolchain_group }}"
        state: present

    - name: Find versioned / glob toolchain binaries
      ansible.builtin.find:
        paths: "{{ t14_toolchain_bin_dir }}"
        file_type: file
        patterns: "{{ t14_toolchain_glob_patterns }}"
      register: t14_toolchain_glob_hits

    - name: Stat exact toolchain paths
      ansible.builtin.stat:
        path: "{{ t14_toolchain_bin_dir }}/{{ item }}"
      loop: "{{ t14_toolchain_exact_basenames }}"
      register: t14_toolchain_exact_stat

    - name: Build combined path list
      ansible.builtin.set_fact:
        t14_toolchain_paths: >-
          {{
            (
              (t14_toolchain_glob_hits.files | default([]) | map(attribute='path') | list)
              + (t14_toolchain_exact_stat.results | default([])
                | selectattr('stat.exists')
                | map(attribute='item')
                | map('regex_replace', '^', t14_toolchain_bin_dir + '/')
                | list)
            ) | unique | list
          }}

    - name: Apply owner, group, mode to toolchain binaries
      ansible.builtin.file:
        path: "{{ item }}"
        owner: "{{ t14_toolchain_owner }}"
        group: "{{ t14_toolchain_group }}"
        mode: "{{ t14_toolchain_file_mode }}"
        follow: true
      loop: "{{ t14_toolchain_paths }}"
      loop_control:
        label: "{{ item }}"

    - name: Install acl package for setfacl (optional ACL tasks)
      when: (t14_toolchain_acl_extra_groups | length) > 0
      ansible.builtin.apt:
        name: acl
        state: present

    - name: Add POSIX ACL entries for extra groups (rx)
      when: (t14_toolchain_acl_extra_groups | length) > 0
      ansible.posix.acl:
        path: "{{ item.0 }}"
        entity: "{{ item.1 }}"
        etype: group
        permissions: rx
        state: present
      loop: "{{ t14_toolchain_paths | product(t14_toolchain_acl_extra_groups) | list }}"
      loop_control:
        label: "{{ item.0 }} g:{{ item.1 }}"
