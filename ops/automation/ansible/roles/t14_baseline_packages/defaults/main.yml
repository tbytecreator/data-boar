---
# Baseline "operator workstation" packages. Keep conservative and common.
t14_baseline_packages:
  # Core system
  - ca-certificates
  - procps
  - curl
  - git
  - gnupg
  - openssh-client
  - openssh-server
  - wget
  - rsync
  # Security hardening
  - auditd
  - audispd-plugins
  - aide
  - debsums
  - needrestart
  - dlocate
  # Process / file inspection
  - lsof
  - strace
  - sysstat
  # Hardware monitoring
  - lm-sensors
  - smartmontools
  # Terminal multiplexer (operator sessions, sudo-warm + tmux send-keys from dev PC)
  - tmux
  # Interactive process monitors
  - htop
  - btop
  - iotop
  - iftop
  - nethogs
  # Disk / filesystem
  - ncdu
  - tree
  - pydf
  # Log viewers
  - lnav
  - multitail
  # Data
  - sqlite3
  - jq
  # Process supervision
  - monit
  # Host metrics collection
  - munin-node
  - prometheus-node-exporter
  # Syslog
  - rsyslog

